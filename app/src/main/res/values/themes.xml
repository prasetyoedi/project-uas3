<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Projectuas3" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Base.Theme.Projectuas3.WithActionBar" parent="Theme.Material3.DayNight" >
        <item name="android:fontFamily">@font/poppinssemibold</item>
        <item name="android:color">@color/white</item>
        <item name="android:popupMenuStyle">@style/MyPopupMenuStyle</item>
    </style>

    <style name="MyPopupMenuStyle" parent="Widget.Material3.PopupMenu">
        <item name="android:popupBackground">@color/blue</item>
    </style>
</resources>